# Backend Environment Variables
NODE_ENV=development
PORT=3001
DATABASE_URL=postgresql://user:password@localhost:5432/renewal_dev
FRONTEND_URL=http://localhost:5173

# Email (Postmark) - Optional in development (emails will be logged to console)
# POSTMARK_API_KEY=your-postmark-server-token
EMAIL_FROM=noreply@renewal.org

# App URL for email links
APP_URL=http://localhost:5173

# Zitadel OIDC Configuration (for token validation)
ZITADEL_ISSUER=https://renewal-initiatives-hgo6bh.us1.zitadel.cloud

# Cross-DB: Financial-System (Neon) — fund allocation + GL staging
# FINANCIAL_SYSTEM_DATABASE_URL=postgres://timesheets_role:password@host/financial_system?sslmode=require

# Cross-DB: App-Portal (Neon) — employee compensation data
# PEOPLE_DATABASE_URL=postgres://timesheets_reader:password@host/app_portal?sslmode=require
